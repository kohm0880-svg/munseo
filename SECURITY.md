# Security

Munseo의 초기 HWP/HWPX 열기는 브라우저 로컬 처리를 기본으로 합니다.

- 실제 개인정보가 들어간 문서를 저장소의 sample/fixture로 커밋하지 않습니다.
- `.env`, 토큰, API 키, 서비스 계정 파일을 커밋하지 않습니다.
- 테스트 문서는 직접 만든 합성 fixture만 사용합니다.
- HWPX는 Munseo 코드가 브라우저에서 직접 파싱합니다.
- HWP 5.x는 필요할 때 버전 고정된 제3자 브라우저 파서 코드를 불러오지만, 사용자가 연 HWP 파일 바이트를 외부 변환 서버로 전송하지 않습니다.
- 향후 서버 저장 기능을 추가할 때는 로컬 전용 모드와 네트워크 저장 모드를 UI에서 명확히 구분합니다.

보안 문제를 공개 issue에 실제 문서나 비밀값과 함께 올리지 마세요.
